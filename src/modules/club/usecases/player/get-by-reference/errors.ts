export enum GetPlayerByReferenceErrors {
    NotFound = 'No player was found with any of the supplied references.',
    ProviderNotSupported = 'None of the external providers supplied are supported.'
}

export default GetPlayerByReferenceErrors;

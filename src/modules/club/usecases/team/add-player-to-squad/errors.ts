export enum AddPlayerToSquadErrors {
    TeamNotFound = 'No squad was found for the team with the teamId specified.',
    PlayerNotFound = 'No player was found with playerId specified.',
}

export default AddPlayerToSquadErrors;

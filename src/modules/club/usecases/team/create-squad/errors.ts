export enum CreateSquadErrors {
    SquadAlreadyExists = 'Team with supplied id already has a squad.',
    TeamNotFound = 'None of the external providers supplied are supported.'
}

export default CreateSquadErrors;
